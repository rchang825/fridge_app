<%= form_with(model: grocery_tagging) do |form| %>
  <% if grocery_tagging.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(grocery_tagging.errors.count, "error") %> prohibited this grocery_tagging from being saved:</h2>

      <ul>
        <% grocery_tagging.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
